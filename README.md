# Аналитическая платформа повторных госпитализаций пациентов с диабетом

## 📋 Описание проекта

Данный проект представляет собой аналитическую платформу для исследования факторов, влияющих на повторные госпитализации пациентов с диабетом. Платформа включает в себя:

- **Сбор и хранение данных** - загрузка и валидация данных о пациентах
- **Анализ данных** - визуализация зависимостей и статистический анализ
- **Прогнозирование** - предсказание вероятности повторной госпитализации на основе данных пациента
- **Интерактивный дашборд** - веб-интерфейс для работы с платформой

## 🎯 Цель проекта

Создание аналитической платформы, которая поможет улучшить бизнес-процессы в здравоохранении за счет:
- Сбора, очистки, предобработки и анализа данных о пациентах с диабетом
- Выявления ключевых факторов, влияющих на повторные госпитализации
- Прогнозирования риска повторной госпитализации для принятия обоснованных решений

## 🏗️ Архитектура проекта
! В проекте есть некоторые скрипты и файлы, которые были нужны в процессе работы над платформой (например, веса модели логистической регрессии больше не используются; некоторые скрипты нужны только для анализа данных в процессе разработки)
```
diabetes_readmission_project/
├── data/                          # Данные проекта
│   ├── raw/   
        └── diabetic_data.csv      # Исходные данные
│   ├── processed/                 # Обработанные данные
│   │   ├── diabetic_data_processed.csv
│   │   ├── diabetic_data_top10.csv           # Чистые данные для БД и модели
│   │   ├── diabetic_data_top10_encoded.csv   # Данные с полной предобработкой
│   │   ├── diabetes.db            # База данных SQLite
│   │   └── schema.json            # Схема данных
│   └── external/                  # Внешние данные (загружаемые пользователем)
│       └── uploaded.csv
├── models/                        # Обученные модели машинного обучения
│   └── catboost_top10.pkl
├── frontend/                      # Веб-интерфейс (Streamlit)
│   ├── app.py
│   └── config.py
├── src/                           # Исходный код
│   ├── api/                       # FastAPI приложение
│   │   ├── app.py                # Главный файл API
│   │   ├── routes/               # Роуты API
│   │   │   ├── dashboard.py      # Роуты для дашборда
│   │   │   ├── predict.py        # Роуты для предсказаний
│   │   │   └── upload_data.py    # Роуты для загрузки данных
│   │   └── utils/                # Утилиты API
│   │       └── visualizations.py # Функции для построения графиков
│   ├── data_processing/          # Обработка данных
│   │   ├── clean_data.py         # Очистка данных
│   │   ├── database.py           # Работа с БД
│   │   ├── feature_engineering.py
│   │   ├── load_data.py          # Загрузка данных
│   │   ├── models.py             # Модели SQLAlchemy
│   │   ├── preprocess.py         # Предобработка
│   │   ├── advanced_preprocessing.py  # Расширенная предобработка с визуализацией
│   │   ├── schema.py             # Схема данных
│   │   └── select_top_features.py
│   ├── modeling/                 # Модели машинного обучения
│   │   ├── predict.py            # Предсказания
│   │   └── train_model.py        # Обучение и оценка моделей
│   └── analysis/                 # Анализ данных
│       ├── data_analysis.py
│       ├── feature_correlation.py
│       └── check_processed_tables.py
├── requirements.txt              # Зависимости проекта
└── README.md                    # Документация
```

## 🚀 Установка и запуск

### Требования

- Python 3.8+
- pip

### Установка зависимостей

```bash
pip install -r requirements.txt
```

### Инициализация данных

**Вариант 1: Полная обработка (если нет готовых данных)**
1. Убедитесь, что исходные данные находятся в `data/raw/diabetic_data.csv`
2. Запустите предобработку данных:

```bash
python -m src.data_processing.preprocess
python -m src.data_processing.select_top_features
```

**Вариант 2: Быстрый запуск (если данные уже обработаны)**
1. Положите готовый файл `diabetic_data_top10.csv` в `data/processed/`
2. Модель `catboost_top10.pkl` в папку `models/`
   (в репозитории данные уже лежат в нужных папках)
3. Инициализируйте базу данных (автоматически при первом запуске API)

### Запуск API сервера

```bash
uvicorn src.api.app:app --reload --host 0.0.0.0 --port 8000
```

API будет доступен по адресу: http://localhost:8000

Документация API (Swagger): http://localhost:8000/docs

### Запуск фронтенда

```bash
streamlit run frontend/app.py
```

Фронтенд будет доступен по адресу: http://localhost:8501

## 📊 Используемые данные

### Топ-10 признаков

Проект использует 10 наиболее значимых признаков для прогнозирования:

**Числовые признаки:**
1. `number_inpatient` - количество стационарных визитов
2. `number_diagnoses` - количество диагнозов
3. `number_emergency` - количество экстренных визитов
4. `number_outpatient` - количество амбулаторных визитов
5. `time_in_hospital` - время пребывания в больнице (дни)

**Категориальные признаки:**
6. `diag_1` - основной диагноз
7. `diag_2` - второй диагноз
8. `diag_3` - третий диагноз
9. `medical_specialty` - медицинская специальность
10. `diabetesMed` - прием диабетических препаратов

**Целевой признак:**
- `readmitted` - повторная госпитализация (NO, <30, >30)

## 🔧 API Endpoints

### Загрузка данных

**POST** `/data/upload`
- Загружает CSV файл с данными пациентов
- Валидирует данные (проверяет наличие всех столбцов и отсутствие пропусков)
- Добавляет данные в базу данных

**Пример запроса:**
```bash
curl -X POST "http://localhost:8000/data/upload" \
  -F "file=@data.csv"
```

### Предсказание

**POST** `/model/predict`
- Принимает данные пациента и возвращает прогноз

**Пример запроса:**
```json
{
  "data": {
    "number_inpatient": 1,
    "number_diagnoses": 5,
    "number_emergency": 0,
    "number_outpatient": 2,
    "time_in_hospital": 5,
    "diag_1": "250.83",
    "diag_2": "250.01",
    "diag_3": "250.04",
    "medical_specialty": "Cardiology",
    "diabetesMed": "Yes"
  }
}
```

### Дашборд

**GET** `/dashboard/stats`
- Возвращает общую статистику по данным

**GET** `/dashboard/chart?chart_type={type}`
- Возвращает график в формате base64
- Доступные типы:
  - `readmission_by_diagnoses` - зависимость от количества диагнозов
  - `readmission_by_inpatient_visits` - зависимость от стационарных визитов
  - `readmission_by_diabetes_med` - зависимость от приема препаратов

**GET** `/dashboard/charts/list`
- Возвращает список доступных графиков

## 🎨 Функциональность фронтенда

### Разделы интерфейса:

1. **📈 Дашборд**
   - Отображение статистики
   - Построение аналитических графиков
   - Выбор типа графика из списка

2. **🔮 Предсказание**
   - Форма ввода данных пациента
   - Получение прогноза вероятности повторной госпитализации
   - Визуализация результатов

3. **📤 Загрузка данных**
   - Загрузка CSV файлов
   - Валидация данных
   - Добавление в базу данных

4. **ℹ️ О проекте**
   - Информация о проекте
   - Проверка подключения к API

## 🗄️ База данных

Проект использует SQLite базу данных для хранения данных с топ-10 признаками.

**Таблица:** `patients_top10`

**Структура:**
- `id` (Integer, Primary Key)
- `number_inpatient` (Integer)
- `number_diagnoses` (Integer)
- `number_emergency` (Integer)
- `number_outpatient` (Integer)
- `time_in_hospital` (Integer)
- `diag_1` (String)
- `diag_2` (String)
- `diag_3` (String)
- `medical_specialty` (String)
- `diabetesMed` (String)
- `readmitted` (String)

База данных автоматически инициализируется при первом использовании и загружает начальные данные из CSV файла.

## 🤖 Модель машинного обучения

Проект использует модель CatBoost для классификации:
- **Тип:** CatBoostClassifier
- **Задача:** Многоклассовая классификация (NO, <30, >30)
- **Признаки:** 10 топ-признаков
- **Файл модели:** `models/catboost_top10.pkl`

## 📝 Обработка ошибок

Все роуты API и фронтенд включают обработку ошибок:
- Валидация входных данных
- Проверка наличия данных в БД
- Обработка ошибок подключения
- Информативные сообщения об ошибках

## 🔒 Валидация данных

При загрузке данных выполняется строгая валидация:
- Проверка наличия всех обязательных столбцов
- Проверка отсутствия пропусков в данных
- Возврат детальных сообщений об ошибках

## 📈 Визуализации

Платформа предоставляет 3 типа графиков:

1. **Зависимость от количества диагнозов**
   - Столбчатая диаграмма
   - Показывает процент повторных госпитализаций по количеству диагнозов

2. **Зависимость от стационарных визитов**
   - Линейный график с заливкой
   - Анализирует связь между количеством визитов и повторными госпитализациями

3. **Зависимость от приема препаратов**
   - Комбинированный график (столбчатая и круговая диаграммы)
   - Сравнивает пациентов с приемом и без приема препаратов

## 🛠️ Технологический стек

- **Backend:**
  - FastAPI - веб-фреймворк
  - SQLAlchemy - ORM для работы с БД
  - Pandas - обработка данных
  - CatBoost - модель машинного обучения
  - Matplotlib/Seaborn - визуализация

- **Frontend:**
  - Streamlit - веб-интерфейс
  - Requests - HTTP клиент

- **База данных:**
  - SQLite - реляционная БД

## 📚 Структура файлов и их назначение

### API (`src/api/`)
- `app.py` - главный файл FastAPI приложения, настройка CORS, обработка ошибок
- `routes/dashboard.py` - роуты для статистики и графиков
- `routes/predict.py` - роуты для предсказаний
- `routes/upload_data.py` - роуты для загрузки данных
- `utils/visualizations.py` - функции для построения графиков

### Обработка данных (`src/data_processing/`)
- `database.py` - настройка БД, инициализация, загрузка начальных данных
- `models.py` - модели SQLAlchemy для работы с БД
- `load_data.py` - функции загрузки данных из БД и CSV
- `preprocess.py` - предобработка исходных данных
- `advanced_preprocessing.py` - расширенная предобработка с анализом выбросов, стандартизацией, нормализацией и визуализацией
- `select_top_features.py` - выбор топ-10 признаков
- `clean_data.py` - очистка данных
- `schema.py` - работа со схемой данных

### Моделирование (`src/modeling/`)
- `train_model.py` - обучение и оценка качества моделей
- `predict.py` - функции предсказания

### Анализ (`src/analysis/`)
- `data_analysis.py` - анализ данных
- `feature_correlation.py` - анализ корреляций признаков

## 🎓 Результаты проекта

1. **Разработанная аналитическая платформа** с полным функционалом:
   - Загрузка и валидация данных
   - Хранение данных в БД
   - Аналитические графики
   - Прогнозирование повторных госпитализаций

2. **Выявленные инсайты:**
   - Факторы, влияющие на повторные госпитализации
   - Зависимости между признаками и целевой переменной

3. **Рекомендации:**
   - Использование платформы для мониторинга пациентов
   - Применение прогнозов для планирования ресурсов
   - Анализ факторов риска для профилактики


При возникновении проблем:
1. Проверьте, что все зависимости установлены
2. Убедитесь, что API сервер запущен
3. Проверьте логи ошибок
4. Убедитесь, что данные в правильном формате

